<?xml version="1.0" encoding="UTF-8"?>
<!--
  launchd service for the BTC 5-minute paper bot.

  SETUP (edit the ONE path, then load):
    1. Replace /Users/USERNAME/btc5m-paper-trader with your real clone path
       in the ProgramArguments <string> below.
    2. Copy this file to ~/Library/LaunchAgents/com.btc5m.paper.plist
    3. launchctl load -w ~/Library/LaunchAgents/com.btc5m.paper.plist
    4. Check it: launchctl list | grep btc5m   (a PID means it's running)
       and tail -f ~/btc5m-paper-trader/bot/bot.log

  STOP / RESTART:
    launchctl unload ~/Library/LaunchAgents/com.btc5m.paper.plist
    launchctl load  -w ~/Library/LaunchAgents/com.btc5m.paper.plist

  KeepAlive=true auto-relaunches the bot if it ever exits or crashes.
  RunAtLoad=true starts it on login/boot.
-->
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>com.btc5m.paper</string>

  <key>ProgramArguments</key>
  <array>
    <string>/bin/bash</string>
    <string>/Users/USERNAME/btc5m-paper-trader/bot/run.sh</string>
  </array>

  <key>RunAtLoad</key>
  <true/>

  <key>KeepAlive</key>
  <true/>

  <key>ThrottleInterval</key>
  <integer>15</integer>

  <key>StandardOutPath</key>
  <string>/tmp/btc5m.out.log</string>
  <key>StandardErrorPath</key>
  <string>/tmp/btc5m.err.log</string>

  <key>ProcessType</key>
  <string>Background</string>
</dict>
</plist>
